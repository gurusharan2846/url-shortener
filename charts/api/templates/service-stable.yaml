apiVersion: v1
kind: Service
metadata:
  name: api-stable
  namespace: {{ include "api.namespace" . }}
spec:
  selector:
    app: api
    track: stable
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  type: ClusterIP